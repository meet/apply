"<%= @call.app_class.model_name.human.titleize %> Applications"
Count,<%= @apps.size %>
Exported,"<%= Time.now %>"

ID<% app_export_columns(@call).each { |c| %>,"<%= @call.app_class.human_attribute_name(c.name) %>"<% } %>
<% @apps.each do |app| %>
<%= app.id %><% app_export_columns(@call).each { |c| %>,"<%= app_response(app, c) %>"<% } %>
<% end %>
