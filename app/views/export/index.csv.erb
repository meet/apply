"<%= @call.app_class.model_name.human.titleize %> Applications"
Count,<%= @apps.size %>
Exported,"<%= Time.now %>"

ID<% @call.app_class.export_columns.each { |c| %>,"<%= @call.app_class.human_attribute_name(c.name) %>"<% } %>
<% @apps.each do |app| %>
<%= app.id %><% @call.app_class.export_columns.each { |c| %>,"<%= app_response(app, c) %>"<% } %>
<% end %>
